# Pin DuckDB below versions where the MotherDuck extension is not yet published
# on extensions.duckdb.org (e.g. 1.5.5 returned HTTP 404 for linux_amd64).
duckdb>=1.2.0,<1.5.5
openpyxl>=3.1.0
pandas>=2.0.0
# Required by DuckDB/MotherDuck when reading timestamptz columns.
pytz>=2024.1
